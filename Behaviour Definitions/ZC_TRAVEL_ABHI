
projection;
strict ( 2 );

define behavior for ZC_Travel_abhi //alias <alias_name>
use etag
{
  use create;
  use update;
  use delete;
  use action CopyTravel;

  use association _booking { create; }
}

define behavior for ZC_booking_abhi //alias <alias_name>
use etag
{
  use update;
  use delete;

  use association _travel;
  use association _booksub { create; }
}

define behavior for ZC_booksuppl_abhi //alias <alias_name>
use etag
{
  use update;
  use delete;

  use association _Travel;
  use association _Booking;
}
